[_top_]
detector = "%{{# Detector:\s+(\w+\s\w+),?(?:\s+SN)?}}"
pixel_size = "# Pixel_size %6e m x %6e m"
exposure_time = "# Exposure_time %8f s"
saturated_value = "# Count_cutoff %7d counts"
wavelength = "# Wavelength %6f A"
distance = "# Detector_distance %7f m"
beam_center = "# Beam_xy (%7f, %7f) pixels"
start_angle =  "# Start_angle %7f deg."
delta_angle = "# Angle_increment %6f deg."
two_theta = "# Detector_2theta %6f deg."
sensor_thickness = "# Silicon sensor, thickness %8f m"
